import { StyleSheet, Text, View } from 'react-native'
import React from 'react'

const CustomTabHeader = () => {
  return (
    <View>
      <Text>CustomTabHeader</Text>
    </View>
  )
}

export default CustomTabHeader

const styles = StyleSheet.create({})